const prices: number[] = [1,2,3];

//I only add in a turple the same type of data and elements
const names: [string,number] = ['nico',1];

//I can't add more than 2 elements
// const names: [string,number] = ['nico',1,2];

//I can't add different types of data
// const names: [string,number] = ['nico',1,true];

//Ican't add less than 2 elements
// const names: [string,number] = ['nico'];

console.log(names)
